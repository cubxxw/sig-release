interface:
  display_name: "Sig Release"
  short_description: "Repo-specific patterns and workflows for sig-release"
  default_prompt: "Use the sig-release repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true